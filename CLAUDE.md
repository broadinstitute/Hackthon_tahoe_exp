# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This repository is currently a bare scaffold. As of the initial commit it contains only an MIT `LICENSE` (Broad Institute). There is no source code, build tooling, dependency manifest, or tests yet.

The directory name (`Hackthon_tahoe_exp`) indicates this is a hackathon experiment project ("Tahoe").

## For future instances

Until code is added, there are no build/lint/test commands or architecture to document. When you add the first substantial code, update this file with:

- The commands to build, run, lint, and test (including how to run a single test).
- The high-level architecture — the big-picture structure that requires reading multiple files to grasp.

Remove this "Status"/"For future instances" placeholder once real content exists.
